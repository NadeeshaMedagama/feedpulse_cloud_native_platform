# repository

Data access interfaces and MongoDB implementation.

- `feedback_repository.go` defines repository contract
- `mongo_feedback_repository.go` handles query filters, indexes, aggregation stats

